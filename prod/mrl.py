"""MRL (Matryoshka Representation Learning) truncation utility.

Placeholder for now

Planned API (subject to change):

    truncate(vectors: np.ndarray, target_dim: int) -> np.ndarray
    canonical_stops() -> list[int]        # {128, 256, 512, 768} for EmbeddingGemma
    is_normalized(vectors: np.ndarray) -> bool
    renormalize(vectors: np.ndarray) -> np.ndarray
"""
